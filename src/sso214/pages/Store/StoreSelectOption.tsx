import { CustomMenuOption } from '../../components';

const StoreSelectOption = () => {
  return (
    <div>
      <CustomMenuOption menu={} handleSubmit={} />
    </div>
  );
};

export default StoreSelectOption;
